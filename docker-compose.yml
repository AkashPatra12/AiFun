services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aifun
      POSTGRES_PASSWORD: aifun
      POSTGRES_DB: aifun
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
